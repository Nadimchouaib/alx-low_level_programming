#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *@n: parameter
 * Return: n if true and -n if false
 */

int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
